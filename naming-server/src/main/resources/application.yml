
server:
  port: 8761
  
spring:
  application:
    name: naming-server

#configurando eureka service para não se registrar
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false